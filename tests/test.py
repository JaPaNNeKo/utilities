import yggdrasil.runner as ygg

if __name__ == "__main__":
    ygg.run("remove",apps="foobar")
    # ygg.run("make",apps="foobar")
    # ygg.run("make", apps="foobar", force_regen=True)
    # ygg.run("update",apps="foobar")
